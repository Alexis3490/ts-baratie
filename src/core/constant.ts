export enum State {
  'Open' = 'Open',
  'Waiting' = 'Waiting',
  'Close' = 'Waiting',
}

export enum DishType {
  Takoyaki = 1,
  Katsudon = 2,
  Udon = 4,
  Ramen = 8,
  MatchaCookie = 16,
}

export enum Dishsize {
  S = 1,
  M = 2,
  L = 4,
  XL = 8,
  XXL = 16,
}

export enum stocks {
  poulpe = 5,
  sauceSoja = 5,
  riz = 5,
  porc = 5,
  oeufs = 5,
  poulet = 5,
  nouilles = 5,
}
